// import { KeyValueCache } from "../src";

describe("index", () => {
  describe("KeyValueCache", () => {
    it("dummy test", () => {
      // Dummy test. In a subsequent PR we will test the actual functionality.

      // const result = new KeyValueCache<{ message: string }>({
      //   prefix: "test",
      //   evictionMillis: 1000,
      //   maxEntries: 100,
      //   maxCacheSize: 1000,
      //   getValueForKey: async key => {
      //     return key;
      //   },
      //   setValueForKey: async (key, value) => {
      //     return;
      //   },
      //   deleteKeyValue: async key => {
      //     return;
      //   },
      //   getAllKeys: async () => {
      //     return [];
      //   },
      //   getKeyFor: async keyParams => {
      //     return keyParams.message;
      //   },
      //   fileExists: async path => {
      //     return false;
      //   },
      //   fileUnlink: async path => {
      //     return;
      //   },
      //   fileSize: async path => {
      //     return 0;
      //   },
      // });

      // const value = await result.delete({ message });

      expect(true).toBe(true);
    });
  });
});
